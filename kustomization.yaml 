apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - app.yaml
# Removing patch moves entire environment to master branch !
# patchesStrategicMerge:
#   - tergetRevision.yaml
